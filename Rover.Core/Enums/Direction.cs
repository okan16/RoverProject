using System.ComponentModel;

namespace Rover.Shared.Enums
{
    // Saga donusler -1
    // Sola donusler +1
    public enum Direction
    {
        E = 0,
        N = 1,
        W = 2,
        S = 3,
        Undefined
    }
}
